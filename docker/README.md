# Docker assets

- `Dockerfile.miner` — cathedral-voice miner sidecar
- `docker-compose.miner*.yml` — sidecar only (ASR/TTS installed on the host)
- `mock-*` / `Dockerfile.mock` — **pytest fixtures only** for sidecar contract tests; not used by miner install/start
